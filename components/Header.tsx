// components/Header.tsx
import styles from '../styles/components/Header.module.css';

const Header = () => {
  return (
    <header className={styles.header}>
      <h1>TDMB</h1>
      {/* Menu dan elemen header lainnya */}
    </header>
  );
};

export default Header;
